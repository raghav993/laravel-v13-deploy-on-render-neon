@extends('layouts.dashboard')
@section('heading','User Management')
@section('content')
<div class="card bg-white p-4"><form class="row g-2 mb-4"><div class="col-md-5"><input name="search" class="form-control" value="{{ request('search') }}" placeholder="Search name, email or phone"></div><div class="col-md-3"><select name="role" class="form-select"><option value="">All roles</option><option value="customer" @selected(request('role')==='customer')>Customers</option><option value="helper" @selected(request('role')==='helper')>Helpers</option><option value="admin" @selected(request('role')==='admin')>Admins</option></select></div><div class="col-md-2"><button class="btn btn-primary w-100">Filter</button></div></form>
<div class="table-responsive"><table class="table align-middle"><thead><tr><th>User</th><th>Contact</th><th>Role</th><th>Joined</th><th>Action</th></tr></thead><tbody>@foreach($users as $u)<tr><td><strong>{{ $u->name }}</strong><div class="small text-muted">#{{ $u->id }}</div></td><td>{{ $u->email }}<br>{{ $u->phone }} @if($u->phone_verified_at)<span class="badge text-bg-success">verified</span>@endif</td><td><span class="badge text-bg-light">{{ $u->role }}</span></td><td>{{ $u->created_at->format('d M Y') }}</td><td><button class="btn btn-sm btn-outline-primary" data-bs-toggle="modal" data-bs-target="#edit{{ $u->id }}">Edit</button></td></tr>
<div class="modal fade" id="edit{{ $u->id }}"><div class="modal-dialog"><form class="modal-content" method="POST" action="{{ route('dashboard.admin.users.update',$u) }}">@csrf @method('PUT')<div class="modal-header"><h5>Edit user</h5><button class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body"><input name="name" class="form-control mb-2" value="{{ $u->name }}" required><input name="email" class="form-control mb-2" value="{{ $u->email }}" required><input name="phone" class="form-control mb-2" value="{{ $u->phone }}"><div class="form-check mb-2"><input class="form-check-input" type="checkbox" name="phone_verified" value="1" id="verified{{ $u->id }}" @checked($u->phone_verified_at)><label class="form-check-label" for="verified{{ $u->id }}">Verified phone</label></div><select name="role" class="form-select"><option value="customer" @selected($u->role==='customer')>Customer</option><option value="helper" @selected($u->role==='helper')>Helper</option><option value="admin" @selected($u->role==='admin')>Admin</option></select></div><div class="modal-footer"><button class="btn btn-primary">Save</button></div></form></div></div>@endforeach</tbody></table></div>{{ $users->links() }}</div>
@endsection
