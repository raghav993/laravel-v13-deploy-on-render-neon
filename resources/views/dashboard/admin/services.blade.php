@extends('layouts.dashboard')
@section('heading','Services & Categories')
@section('content')
<div class="row g-4"><div class="col-lg-4"><div class="card bg-white p-4"><h5>Add service</h5><form method="POST" action="{{ route('dashboard.admin.services.store') }}">@csrf<select name="service_category_id" class="form-select mb-2" required>@foreach($categories as $c)<option value="{{ $c->id }}">{{ $c->name }}</option>@endforeach</select><input name="name" class="form-control mb-2" placeholder="Service name" required><input name="name_hi" class="form-control mb-2" placeholder="Hindi name"><input name="slug" class="form-control mb-2" placeholder="unique-slug" required><textarea name="description" class="form-control mb-2" placeholder="Description"></textarea><div class="form-check mb-3"><input class="form-check-input" type="checkbox" name="is_active" value="1" checked><label class="form-check-label">Active</label></div><button class="btn btn-primary w-100">Add service</button></form></div></div>
<div class="col-lg-8">@foreach($categories as $c)<div class="card bg-white p-4 mb-3"><div class="d-flex justify-content-between"><h5>{{ $c->name }} <span class="badge text-bg-light">{{ $c->services->count() }}</span></h5></div>@foreach($c->services as $s)<div class="d-flex justify-content-between align-items-center border-top py-3"><div><strong>{{ $s->name }}</strong><div class="small text-muted">{{ $s->name_hi }} · {{ $s->slug }}</div></div><div class="d-flex gap-2"><span class="badge text-bg-{{ $s->is_active?'success':'secondary' }}">{{ $s->is_active?'Active':'Disabled' }}</span><button class="btn btn-sm btn-outline-primary" data-bs-toggle="modal" data-bs-target="#svc{{ $s->id }}">Edit</button><form method="POST" action="{{ route('dashboard.admin.services.delete',$s) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('Remove this service?')">Delete</button></form></div></div><div class="modal fade" id="svc{{ $s->id }}"><div class="modal-dialog"><form class="modal-content" method="POST" action="{{ route('dashboard.admin.services.update',$s) }}">@csrf @method('PUT')<div class="modal-header"><h5>Edit service</h5><button class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body"><select name="service_category_id" class="form-select mb-2">@foreach($categories as $cc)<option value="{{ $cc->id }}" @selected($s->service_category_id===$cc->id)>{{ $cc->name }}</option>@endforeach</select><input name="name" class="form-control mb-2" value="{{ $s->name }}"><input name="name_hi" class="form-control mb-2" value="{{ $s->name_hi }}"><input name="slug" class="form-control mb-2" value="{{ $s->slug }}"><textarea name="description" class="form-control mb-2">{{ $s->description }}</textarea><div class="form-check"><input class="form-check-input" type="checkbox" name="is_active" value="1" @checked($s->is_active)><label class="form-check-label">Active</label></div></div><div class="modal-footer"><button class="btn btn-primary">Save</button></div></form></div></div>@endforeach</div></div>@endforeach</div></div>
@endsection
